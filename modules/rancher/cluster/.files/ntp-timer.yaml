systemd:
  units:
    - name: ntp-timer.service
      enabled: true
      contents: |
        [Timer]
        OnCalendar=*:0/15
        RandomizedDelaySec=120
        OnBootSec=1min

        [Install]
        WantedBy=timers.target
