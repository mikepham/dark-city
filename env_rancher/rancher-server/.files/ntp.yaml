systemd:
  units:
    - name: ntp.service
      enabled: true
      contents: |
        [Service]
        Type=oneshot
        ExecStart=/sbin/ntpdate ${ntp_host}

        [Install]
        WantedBy=multi-user.target
