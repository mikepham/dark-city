systemd:
  units:
    - name: ntp.service
      enabled: true
      contents: |
        [Unit]
        Requires=network.service

        [Service]
        Type=oneshot
        ExecStart=/sbin/ntpdate ${ntp_host}

        [Install]
        WantedBy=multi-user.target
